spring:
  datasource:
    url: jdbc:h2:mem:tasktracker-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: tasktracker-test-secret-key-tasktracker-2025
    expiration: 3600000
  bootstrap:
    admin:
      username: admin
      password: admin12345
